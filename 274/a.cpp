#include <bits/stdc++.h>

using namespace std;
#define ull unsigned long long
#define ll long long

#define rep(i,n) for (int i=0; i<(n); i++)

int main() {
    double A, B;
    cin >> A >> B;
    cout << fixed << setprecision(3) << B / A << endl;
    return 0;
}